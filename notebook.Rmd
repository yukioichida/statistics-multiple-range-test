---
title: "Duncan's new multiple range test"
output:
  html_document:
    df_print: paged
---

### A primeira coisa a ser feita é carregar os dados em um DataFrame

```{r}
data = read.csv(file = "data.csv", row.names = 1, stringsAsFactors = TRUE)
data

```

### Antes de executarmos o MRT, precisamos executar ANOVA. Iremos guardar o resultado a ANOVA em um novo dataframe

```{r}
model<-lm(yeld~treatment, data)
anova1  = anova(model)
anova1
```

### Execução do MRT
```{r}
#library(DescTools)
#PostHocTest(anova1, method = "duncan")
library(agricolae)
duncan.test(anova1,"Dieta",alpha=0.05)
#plot(results,horiz=TRUE,las=1)
```

